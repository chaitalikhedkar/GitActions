pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building project...'
    
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                
            }
        }
    }

    post {
        success {
            echo '✅ Build Successful!'
        }
        failure {
            echo '❌ Pipeline failed. Please check the logs.'
        }
    }
}
